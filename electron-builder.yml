appId: com.vinewoodlegacy.streamingbot
productName: StreamingBot
directories:
  buildResources: build
files:
  - out/**/*
  - '!**/.vscode/*'
  - '!src/*'
  - '!electron.vite.config.{js,ts,mjs,cjs}'
asarUnpack:
  - resources/**
win:
  target:
    - target: nsis
      arch:
        - x64
  # Code-Signing bewusst noch nicht aktiv (siehe README "Deployment").
  # Sobald ein Zertifikat vorliegt, hier ergänzen:
  # certificateFile: ${env.WIN_CSC_LINK}
  # certificatePassword: ${env.WIN_CSC_KEY_PASSWORD}
nsis:
  oneClick: false
  allowToChangeInstallationDirectory: true
  createDesktopShortcut: always
publish:
  provider: github
  # release-please veroeffentlicht das GitHub-Release bereits vor diesem Build
  # (nicht als Draft). Ohne releaseType: release erwartet electron-builder ein
  # Draft-Release mit diesem Tag und bricht den Asset-Upload sonst mit
  # "existing type not compatible with publishing type" stillschweigend ab.
  releaseType: release
